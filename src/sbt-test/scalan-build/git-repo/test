# init git repo
$ exec git init
$ exec git config user.email "you@example.com"
$ exec git config user.name "Your Name"
$ exec git add .
$ exec git commit -m 'initial commit'

# check versionFixed
> versionFixed

# git revision should succeed
> gitRevision
